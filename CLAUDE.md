# CLAUDE.md

This project keeps all AI-agent guidance in **`AGENTS.md`** (agent-neutral,
so the same rules apply whichever coding agent is in use).

**Read `AGENTS.md` now** — it is the single source of truth for project
orientation (stack, commands, architecture, auth, testing), the mandatory
instruction-kit loading order, and the non-negotiable rules. Claude Code
does not auto-load `AGENTS.md`, so load it at the start of every session.

Also read the source-of-truth documents it points to: `contract.md`
(authoritative spec), `TODO.md` (backlog).
